/** GENERATED — real-schema geography authoring blocks. */
import { manifestToBlock } from '../../lab-def/to-block.js';
import cycle from '../geography/cycle/manifest.js';

export const blocks = [cycle].map(manifestToBlock);
