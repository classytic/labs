/** GENERATED — real-schema exam authoring blocks. */
import { manifestToBlock } from '../../lab-def/to-block.js';
import examQuestion from '../exam/exam-question/manifest.js';

export const blocks = [examQuestion].map(manifestToBlock);
