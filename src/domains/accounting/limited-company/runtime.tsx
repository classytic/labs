'use client';

/** Limited liability (sole trader vs limited company) runtime — its own lazy chunk (engine-backed component). */
export { LimitedCompanyLab as default } from '../../../commerce/finance/index.js';
