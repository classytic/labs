'use client';

/** Financial statements (income statement → balance sheet) runtime — its own lazy chunk (engine-backed component). */
export { StatementBuilderLab as default } from '../../../commerce/finance/index.js';
