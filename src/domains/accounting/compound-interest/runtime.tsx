'use client';

/** Compound interest (the money snowball) runtime — its own lazy chunk (engine-backed component). */
export { CompoundInterestLab as default } from '../../../commerce/finance/index.js';
