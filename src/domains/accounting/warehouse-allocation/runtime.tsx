'use client';

/** Warehouse cost allocation (by floor area) runtime — its own lazy chunk (engine-backed component). */
export { WarehouseAllocationLab as default } from '../../../commerce/finance/index.js';
