'use client';

/** Depreciation (straight-line vs reducing-balance) runtime — its own lazy chunk (engine-backed component). */
export { DepreciationLab as default } from '../../../commerce/finance/index.js';
