'use client';

/** Stock control, when to reorder (sawtooth) runtime — its own lazy chunk (engine-backed component). */
export { ReorderPointLab as default } from '../../../commerce/finance/index.js';
