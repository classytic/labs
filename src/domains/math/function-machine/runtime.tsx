'use client';

/** Function machine (find the rule) runtime — its own lazy chunk (engine-backed component). */
export { FunctionMachineLab as default } from '../../../math/function-machine/index.js';
