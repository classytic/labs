'use client';

/** Proportion machine (drag the count, it scales) runtime — its own lazy chunk (engine-backed component). */
export { RateMachineLab as default } from '../../../math/rate-machine/index.js';
