'use client';

/** Straight-line runtime — its own lazy chunk (engine-backed component; attributes map to props). */
export { StraightLineLab as default } from '../../../math/straight-line/index.js';
