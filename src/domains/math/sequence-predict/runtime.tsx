'use client';

/** Exponential / sequence (watch it grow) runtime — its own lazy chunk (engine-backed component). */
export { SequencePredict as default } from '../../../math/sequence-predict/index.js';
