'use client';

/** Polynomial solver (factor / solve, step by step) runtime — its own lazy chunk (engine-backed component). */
export { PolynomialSolverLab as default } from '../../../math/poly/index.js';
