'use client';

/** Place-value dial (count in any base) runtime — its own lazy chunk (engine-backed component). */
export { PlaceValueDialLab as default } from '../../../ict/number-systems/index.js';
