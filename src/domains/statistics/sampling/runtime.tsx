'use client';

/** Sampling distribution & confidence intervals runtime — its own lazy chunk (engine-backed component). */
export { SamplingDistributionLab as default } from '../../../statistics/sampling/preset.js';
