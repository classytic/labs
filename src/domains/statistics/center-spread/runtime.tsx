'use client';

/** Centre & spread (mean as balance point) runtime — its own lazy chunk (engine-backed component). */
export { CenterSpreadLab as default } from '../../../statistics/center-spread/preset.js';
