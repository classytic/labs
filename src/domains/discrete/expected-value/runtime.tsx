'use client';

/** Expected value (is the game fair?) runtime — its own lazy chunk (engine-backed component). */
export { ExpectedValueLab as default } from '../../../discrete/expected-value/preset.js';
