'use client';

/** Draw from the bag (cards & colored balls) runtime — its own lazy chunk (engine-backed component). */
export { SelectionLab as default } from '../../../discrete/selection/preset.js';
