'use client';

/** Sample-space builder (coins & dice) runtime — its own lazy chunk (engine-backed component). */
export { OutcomeBuilderLab as default } from '../../../discrete/outcome-builder/preset.js';
