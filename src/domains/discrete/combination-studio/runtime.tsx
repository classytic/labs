'use client';

/** Combination studio (rule of product, felt) runtime — its own lazy chunk (engine-backed component). */
export { CombinationStudioLab as default } from '../../../discrete/combination-studio/index.js';
