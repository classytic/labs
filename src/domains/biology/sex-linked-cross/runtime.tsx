'use client';

/** Sex-linked cross (X-linked traits) runtime — its own lazy chunk (engine-backed component). */
export { SexLinkedCrossLab as default } from '../../../biology/genetic-cross/index.js';
