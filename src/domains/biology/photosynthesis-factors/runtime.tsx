'use client';

/** Photosynthesis: limiting factors runtime — its own lazy chunk (engine-backed component). */
export { PhotosynthesisFactorsLab as default } from '../../../biology/photosynthesis-factors/index.js';
