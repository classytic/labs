'use client';

/** Photosynthesis ⇌ Respiration (the mirror) runtime — its own lazy chunk (engine-backed component). */
export { RespirationLab as default } from '../../../biology/respiration/index.js';
