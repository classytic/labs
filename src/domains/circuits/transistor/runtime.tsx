'use client';

/** Transistor: a small input controls a big current runtime — its own lazy chunk (engine-backed component). */
export { TransistorLab as default } from '../../../circuits/transistor/index.js';
