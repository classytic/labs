'use client';

/** Why current flows (drift + Ohm's law) runtime — its own lazy chunk (engine-backed component). */
export { ConductionLab as default } from '../../../circuits/semiconductor/index.js';
