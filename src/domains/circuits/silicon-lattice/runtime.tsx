'use client';

/** What is a semiconductor? (Si lattice + doping) runtime — its own lazy chunk (engine-backed component). */
export { SiliconLatticeLab as default } from '../../../circuits/semiconductor/index.js';
