'use client';

/** The Hall effect (electrons vs holes) runtime — its own lazy chunk (engine-backed component). */
export { HallEffectLab as default } from '../../../circuits/semiconductor/index.js';
