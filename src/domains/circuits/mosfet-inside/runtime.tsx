'use client';

/** Inside the transistor (NMOS channel) runtime — its own lazy chunk (engine-backed component). */
export { MosfetInsideLab as default } from '../../../circuits/semiconductor/index.js';
