'use client';

/** Rain on a moving car (relative velocity) runtime — its own lazy chunk (engine-backed component). */
export { RainRelativeLab as default } from '../../../physics/rain-relative/index.js';
