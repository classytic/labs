'use client';

/** Heat transfer, conduction / convection / radiation runtime — its own lazy chunk (engine-backed component). */
export { HeatTransferLab as default } from '../../../physics/heat-transfer/index.js';
