'use client';

/** Heating curve, q=mcΔθ runs + latent plateaus runtime — its own lazy chunk (engine-backed component). */
export { HeatingCurveLab as default } from '../../../physics/thermal/index.js';
