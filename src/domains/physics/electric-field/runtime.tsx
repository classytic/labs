'use client';

/** Electric field, charges & the force F = qE runtime — its own lazy chunk (engine-backed component). */
export { ElectricFieldLab as default } from '../../../physics/electric-field/index.js';
