'use client';

/** Electric flux Φ = E·A·cosθ (line-counting) runtime — its own lazy chunk (engine-backed component). */
export { ElectricFluxLab as default } from '../../../physics/electric-flux/index.js';
