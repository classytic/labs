'use client';

/** Terminal velocity, the skydiver (air drag) runtime — its own lazy chunk (engine-backed component). */
export { TerminalVelocityLab as default } from '../../../physics/terminal-velocity/index.js';
