'use client';

/** Refraction & total internal reflection (Snell’s law) runtime — its own lazy chunk (engine-backed component). */
export { RefractionLab as default } from '../../../physics/optics/index.js';
