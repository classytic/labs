'use client';

/** Reflection & standing waves on a string runtime — its own lazy chunk (engine-backed component). */
export { StringReflectionLab as default } from '../../../physics/waves/index.js';
