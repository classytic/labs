'use client';

/** Bullet through N planks (penetration) runtime — its own lazy chunk (engine-backed component). */
export { BulletWallsLab as default } from '../../../physics/bullet-walls/index.js';
