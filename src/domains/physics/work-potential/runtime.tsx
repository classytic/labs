'use client';

/** Potential & work, equipotentials and W = qΔV runtime — its own lazy chunk (engine-backed component). */
export { WorkPotentialLab as default } from '../../../physics/work-potential/index.js';
