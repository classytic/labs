'use client';

/** Work done = area under the force–distance graph runtime — its own lazy chunk (engine-backed component). */
export { WorkEnergyLab as default } from '../../../physics/work-energy/index.js';
