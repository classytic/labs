'use client';

/** Water’s 4 °C anomaly, why ice floats runtime — its own lazy chunk (engine-backed component). */
export { WaterDensityLab as default } from '../../../physics/water-density/index.js';
