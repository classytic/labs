'use client';

/** Universal gravitation, the inverse-square law runtime — its own lazy chunk (engine-backed component). */
export { GravitationLab as default } from '../../../physics/gravitation/index.js';
