'use client';

/** Collision track (momentum & elasticity) runtime — its own lazy chunk (engine-backed component). */
export { CollisionTrackLab as default } from '../../../physics/collision-track/index.js';
