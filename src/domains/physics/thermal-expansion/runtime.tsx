'use client';

/** Thermal expansion, length / area / volume / bimetallic runtime — its own lazy chunk (engine-backed component). */
export { ThermalExpansionLab as default } from '../../../physics/expansion/index.js';
