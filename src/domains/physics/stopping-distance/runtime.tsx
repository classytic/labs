'use client';

/** Stopping distance (drive & brake) runtime — its own lazy chunk (engine-backed component). */
export { StoppingDistanceLab as default } from '../../../physics/stopping-distance/index.js';
