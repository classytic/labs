'use client';

/** Circular motion, centripetal force & cut the string runtime — its own lazy chunk (engine-backed component). */
export { CircularMotionLab as default } from '../../../physics/circular-motion/index.js';
