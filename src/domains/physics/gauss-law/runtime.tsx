'use client';

/** Gauss’s law, flux depends only on charge inside runtime — its own lazy chunk (engine-backed component). */
export { GaussLab as default } from '../../../physics/gauss-law/index.js';
