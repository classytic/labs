'use client';

/** Temperature scales, °C / °F / K runtime — its own lazy chunk (engine-backed component). */
export { TemperatureScalesLab as default } from '../../../physics/temperature-scales/index.js';
