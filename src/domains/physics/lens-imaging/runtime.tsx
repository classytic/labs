'use client';

/** Lens imaging (three rays & 1/f = 1/u + 1/v) runtime — its own lazy chunk (engine-backed component). */
export { LensImagingLab as default } from '../../../physics/optics/index.js';
