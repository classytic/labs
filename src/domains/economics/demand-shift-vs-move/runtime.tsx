'use client';

/** Demand shift-vs-move runtime — its own lazy chunk (engine-backed component, props match). */
export { DemandShiftVsMoveLab as default } from '../../../commerce/economics/index.js';
