# Harness audit — 2026-09-06

## Mode

Initial build. The repository had project-specific `lab-design` and `ui-ux-pro-max` skills, but no reusable agent definitions or visual-proof orchestrator.

## Team

- Spatial Biology: cell-division proof.
- Physics Visualization: relativity reference-frame proof.
- Interaction Systems: semiconductor junction and representative 2D mathematics proof.
- Root: canonical runtime, shared CSS, boundary QA, integration, and consumer sync.

## Shared constraints

- One canonical `AuthoredActivityRuntime`; no versioned shells.
- Host shadcn controls for generic UI; domain graphics remain semantic scene code.
- Heavy renderers stay optional, lazy, and domain-local.
- Shared primitives require evidence from at least three proofs.
- Visual quality remains unverified until a rendered view is inspected.

## Model compatibility

The source Harness requests an `opus` model. That identifier is unavailable in this execution environment, so agents inherit the available capable model rather than declaring an unsupported override.

## Proof-run result

- Spatial biology: mitosis sequence, prediction and attachment gates, meaningful SVG fallback, and higher-contrast WebGL scene completed.
- Physics visualization: light-clock, simultaneity, length-contraction, and Lorentz-frame scenes now change with canonical sequence progression; prediction gates restored.
- Interaction systems: PN-junction physics/authoring corrected and the walking-path measurement proof made directly manipulable.
- Shared runtime: scene controls now sit beside the model, evidence stays contextual, step state can initialize/synchronize domain scenes, and gated prediction conclusions remain hidden until commitment.
- CSS ownership: measurement-only scene roles moved from `core.css` to `domains.css`.

## Verification status

- Verified: focused proof tests, TypeScript, host-shadcn conformance, package build, and diff whitespace checks.
- Pending: browser-rendered 360 px label fit, WebGL lighting, and visual contrast. These require the consumer's live visual pass and must not be inferred from DOM tests.
